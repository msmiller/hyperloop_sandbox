<%= form_for(blivot) do |f| %>
  <% if blivot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blivot.errors.count, "error") %> prohibited this blivot from being saved:</h2>

      <ul>
      <% blivot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :priority %>
    <%= f.number_field :priority %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
